[project]
name = "ds-lb-analysis"
version = "0.1.0"
description = "Task 4: load-distribution and failure-recovery experiments for the load balancer"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "matplotlib>=3.8",
]

[tool.uv]
package = false
